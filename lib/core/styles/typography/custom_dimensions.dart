import 'package:flutter/material.dart';
import 'package:coin_market/resources/colors.dart';

class CustomDimensions {
  static const largeMargin = 24.0;
  static const defaultMargin = 16.0;
  static const defaultPadding = 16.0;
  static const buttonTextSize = 16.0;

  //Borders
  static const borderSide = BorderSide(color: AppColors.neutral600);
  static const thickBorderSide = BorderSide(
    color: AppColors.neutral600,
    width: 1.5,
  );
}
